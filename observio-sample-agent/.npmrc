# Pre-existing peer conflict: @langchain/aws@0.1 requires @langchain/core <0.4 while
# @langchain/langgraph/@langchain/core here are 1.x. npm ci fails without this flag
# (a lockfile produced with --legacy-peer-deps must be installed with it too).
legacy-peer-deps=true
