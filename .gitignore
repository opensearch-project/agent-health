node_modules
/dist
server/dist
cli/dist
lib/dist/
build

# Environment variables
.env
.env.*
.env.backup*
!.env.example
!.env.docker
docker-compose/.env
judge.log

# Configuration files (may contain credentials)
agent-health.config.ts
agent-health.config.js
agent-health.config.mjs
agent-health.config.json

/cli/dist

# macOS
.DS_Store

# Backup files
*.backup*

# Test coverage
/coverage/

# Playwright
/playwright-report/
/playwright/.cache/
/test-results/

/plans

tokens.json

# Local auth tokens
.auth/

# Claude Code working files (keep skills shared)
.claude/*
!.claude/skills/

# Development artifacts
*.local.json

# App-managed (runtime, not source): config-v2 state + generated data,
# combined under .agent-health/ (state.json + data/).
.agent-health/

# AI-agent generated specification files (keep steering shared)
.kiro/*
!.kiro/steering/
# pi-web local agent state (artifacts, uploads)
.pi/

# V-suffixed cp-oncall evals are local sandboxes / SDK feature tours;
# they pollute storage with throwaway test cases on every run.
evals/cp-oncall-V*.eval.js
