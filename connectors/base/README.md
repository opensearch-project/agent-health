# Connector base class

`BaseConnector` provides auth, trace-context propagation, trajectory-step creation, and health-check helpers shared by connector implementations. It is not a registry entry. See [`../README.md`](../README.md) for the connector contract.
